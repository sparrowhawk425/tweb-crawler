# Typescript Webcrawler

A simple web crawler that pulls internal links and images from web pages. Run from the commandline, you pass in the starting URL, max connections allowed for concurrency and the max unique pages to scrape. Once done crawling, it generates a report: report.json.
